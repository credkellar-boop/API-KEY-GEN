# Contributing to API-KEY-GEN

Thank you for your interest in improving API-KEY-GEN! We welcome bug reports, feature requests, and pull requests.

## Local Development

Since this repository contains implementations in three different languages, you only need to set up the environment for the language you wish to modify.

### Python
1. Ensure Python 3.8+ is installed.
2. Run tests:
   ```bash
   python -m unittest discover -s tests
   
